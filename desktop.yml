---
- name: Configure my personal hosts
  hosts: localhost
  become: true
  connection: local 
  roles:
    - repos
    - misc-base
    - misc-desktop
    - git
    - users
    - plymouth
    - sensors
    - sway
    - terminal
    - vim
    - zsh
    - gdb
